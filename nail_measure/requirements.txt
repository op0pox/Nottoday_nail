# Jetson Nano에는 보통 OpenCV(python3-opencv)가 시스템 패키지로 이미 설치되어 있습니다.
# JetPack 이미지의 OpenCV는 pip으로 다시 설치하면 GStreamer(CSI 카메라) 지원이
# 빠진 버전이 깔릴 수 있으므로, 아래 opencv-python은 "시스템에 OpenCV가 전혀 없는
# 경우"에만 설치하세요. (자세한 내용은 README.md의 '패키지 설치' 항목 참고)

numpy>=1.19.0

# CSI 카메라(Raspberry Pi Camera Module 2/3, Arducam CSI)만 쓸 경우 아래 opencv-python은
# 설치하지 마세요. Jetson Nano 기본 OpenCV(GStreamer 지원 포함)를 그대로 사용해야 합니다.
# USB 카메라만 쓰고 시스템에 OpenCV가 없는 경우에만 주석을 해제하세요.
# opencv-python>=4.5.0
